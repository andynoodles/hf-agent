## Python environment
- Use .venv as main environment for this project
- Write needed dependencies in `requirements.txt`
- Use `uv pip install -r requirements.txt` to install dependencies

## Running the project
- Use `uv run main.py` to start the project
